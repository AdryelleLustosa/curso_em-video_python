# Reajuste salarial

salario_atual = float(input('Qual seu salario atual? '))
salario_reajustado = float(salario_atual + (salario_atual * 0.15))

print('Seu salario foi reajustado de {} reais para {} reais'.format(salario_atual, salario_reajustado))